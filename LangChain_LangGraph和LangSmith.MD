1. LangChain（胶水框架 / 基础工具箱）
官方定义：一个用于开发大语言模型（LLM）应用的开源框架。

核心作用：连接与封装。它把零散的AI能力（模型、工具、数据库）包装成统一的Python对象，让你写代码更优雅。

2. LangGraph（流程控制 / 底层大脑）
官方定义：一个基于图结构（Graph） 来构建、编排复杂工作流的库（属于LangChain生态）。

核心作用：编排与决策。没有它，Agent只能走直线；有了它，Agent可以走迷宫、做循环，甚至拥有自主规划路径的能力。

3. LangSmith（可观测性 / 监控调试台）
官方定义：一个用于调试、测试、评估和监控LLM应用全生命周期的SaaS平台（收费，但有免费额度）。

核心作用：黑盒可视化。LLM是概率模型，极其脆弱。没有LangSmith，你只能用 print() 打日志，面对复杂的Agent链路会陷入“盲人摸象”的困境。

4. RAG（检索增强生成）
官方定义：一种技术范式，指在LLM生成回答前，先从外部知识库（如向量数据库）检索相关文档，作为上下文注入给模型。
核心作用：解决幻觉与知识滞后。让模型“开卷考试”，确保输出有据可查，是企业落地AI的绝对刚需。

5. MCP（模型上下文协议）
官方定义：由Anthropic（Claude公司）提出的开放标准协议，旨在统一LLM应用与外部数据源/工具之间的连接方式。
核心作用：解耦与标准化。以后开发者只需写一个MCP Server，所有支持MCP的AI应用（Claude Desktop、LangChain）都能直接拿来用，打破数据孤岛，避免为每个工具写重复的适配代码。

6. 当你开发一个完整的智能客服时，它们的协作顺序是：

开发阶段：用 LangChain 定义工具和Prompt模板；用 LangGraph 把“推理-查知识库-再推理”的循环画出来。

知识注入：用 RAG 流程把公司手册切块后存入向量库。

测试阶段：把代码跑起来，所有轨迹自动上传到 LangSmith，查看是哪一步Prompt写错了导致模型抽风。

进阶扩展：如果要连接公司内部的ERP系统，不用再写LangChain自定义工具，而是直接搭建一个 MCP Server，让Agent通过标准协议对接。

这五个模块组合在一起，就构成了一个可观测、可控制、有记忆、有知识、易扩展的下一代智能体系统。


视频链接：[https://www.bilibili.com/list/watchlater?oid=117037892370817&bvid=BV1AkMB6qEV1&spm_id_from=333.788.top_right_bar_window_view_later.content.click&vd_source=a8e43ca5a606e00759cb33660e3646da]